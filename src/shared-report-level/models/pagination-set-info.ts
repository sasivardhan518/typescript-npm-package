import { ReportFormat } from './report-format';
import { TransformedData } from './transformed-data';
export interface PaginationSetInfo {
    serverData: TransformedData;
    reportFormat: ReportFormat;
}
