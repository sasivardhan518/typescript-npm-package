export enum ColumnType {
    Regular = 0,
    Stacked = 1,
    Report = 2,
    Total = 3,
    Hierarchy = 4,
    Special = 5,
    Unknown = 6, // can be used for Standard report columns
    Group = 7
}
